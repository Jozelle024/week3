/*jshint esversion: 6 */

const libreria = require('./libreria.js'); // if anyone wants to use the library we defined
const area = libreria.area(4);
console.log(`L'area è ${area}`);

const superficie = libreria.cirtconferenza(6);
console.log(`La superficie è ${superficie}`);